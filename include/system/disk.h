/* This is for showing everything about the disk[s] on startup */

#ifndef FOMOSV2_CL_DISK_H
#define FOMOSV2_CL_DISK_H

#include <stdio.h>

int disk(void) {
    int disk = 0;
    printf("Disk %d\n", disk);
    return 0;
}

#endif //FOMOSV2_CL_DISK_H
